spring:
  application:
    name: bank_account
  data:
    mongodb:
      uri: "mongodb+srv://romcel87_db_user:OpUT3j2jbzWKCB69@cluster0.xgobikv.mongodb.net/bank_account?retryWrites=true&w=majority&tls=true"

server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

springdoc:
  api-docs.path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
    tagsSorter: alpha

    
customer-service:
  base-url: http://customer/customers
  

account:
  rules:
    savings:
      monthly-movement-limit: 10        # cambia a tu gusto
    current:
      maintenance-fee: 12.50            # comisión mensual
    fixed-term:
      allowed-day-of-month: 15          # día permitido (1..28/31)




